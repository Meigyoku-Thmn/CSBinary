{
  "name": "csbinary",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "ts-mocha -p tsconfig.json test/**/*.spec.ts",
    "test:coverage": "nyc --reporter=html --reporter=text --reporter=text-summary npm test",
    "config": "node-gyp configure",
    "build": "node-gyp build && tsc",
    "rebuild": "node-gyp rebuild && tsc -b --clean && rimraf types && tsc",
    "clean": "node-gyp clean && rimraf dist && rimraf types"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/bindings": "^1.3.0",
    "@types/fs-ext": "^2.0.0",
    "@types/lodash": "^4.14.157",
    "@types/mocha": "^7.0.2",
    "@types/mock-fs": "^4.10.0",
    "@types/mock-require": "^2.0.0",
    "@types/node": "^14.0.14",
    "lodash": "^4.17.19",
    "mocha": "^8.0.1",
    "mock-fs": "github:Meigyoku-Thmn/mock-fs#master",
    "mock-require": "^3.0.3",
    "nyc": "^15.1.0",
    "rimraf": "^3.0.2",
    "ts-mocha": "^7.0.0",
    "typescript": "^3.9.6"
  },
  "dependencies": {
    "bindings": "^1.5.0",
    "fs-ext": "github:Meigyoku-Thmn/node-fs-ext#master",
    "node-addon-api": "*"
  },
  "gypfile": true
}
